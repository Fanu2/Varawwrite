{
  "name": "vara-project",
  "version": "1.0.0",
  "description": "A project using Vara.js for animated handwriting text",
  "main": "index.js",
  "scripts": {
    "start": "parcel index.html --no-hmr",
    "build": "parcel build index.html"  // Added build script for Vercel
  },
  "dependencies": {
    "vara": "^1.4.1"
  },
  "devDependencies": {
    "parcel": "^2.9.3"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=14.0.0"  // Optional: Specify Node.js version
  }
}
